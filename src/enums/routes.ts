// 首页
export enum Home {
  BASE = '/'
}

// 关于
export enum About{
  BASE = '/about',
}

// 博客
export enum Blocks{
  BASE = '/blocks'
}

// 文档
export enum Docs {
  BASE = '/docs'
}

// 其他平台
export enum Other {
  BASE = '/other',
  XHS = '/other/xhs',
  WB = '/other/wb',
  DY = '/other/dy'
}
